on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: touch $RANDOM && git add . && git commit --amend --no-edit && git push --force
